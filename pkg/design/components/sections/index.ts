/**
 * Design system sections
 * Composite UI blocks (theme preview, customizer panels, etc.)
 * Add section components here and re-export.
 */

export { Header } from './header'
export type { HeaderProps, HeaderLink } from './header'
